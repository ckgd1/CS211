#include <iostream> 

using namespace std;

void myStrcpy(char* dest, const char* source);

int main()
{


  return 0;
}

void myStrcpy(char* dest, const char* source)
{
  int i;
  for(i = 0; source[i] != '\0'; i++)
    dest[i] = source[i];
  det[i] = '\0';
}
